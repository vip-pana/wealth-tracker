{"_schema": "adr", "_version": "1.0", "_description": "Architectural decision records: routing, state management, build pipeline, major library choices, cross-cutting refactors. Append only; supersede with a new entry instead of editing.", "_fields": ["id", "title", "status", "context", "decision", "rationale", "consequences_positive", "consequences_negative", "supersedes", "date"]}
{"id": "adr_20260530_213923_014a", "title": "No REST API — Inertia.js with server-rendered React", "status": "accepted", "context": "Single-user local app; a separate JSON API plus a client fetch layer would add ceremony with no payoff.", "decision": "Controllers return Inertia::render() for GET and redirect()->back() for mutations; React pages receive typed props directly. No JSON API.", "rationale": "Removes the API/client contract and serialization boundary; one source of truth for props; simpler to keep TypeScript types aligned.", "consequences_positive": ["Less code", "No API versioning", "Typed props end-to-end"], "consequences_negative": ["No external/programmatic API surface", "Coupled to Inertia"], "supersedes": "", "date": "20260530_213923"}
{"id": "adr_20260530_213923_014a", "title": "Soft deletes plus toast-level undo for user data", "status": "accepted", "context": "An assistant once deleted real financial data; deletions must be recoverable.", "decision": "Core tables (assets, categories, goals, pension) use SoftDeletes; destroy controllers flash an undo URL that a restore endpoint consumes.", "rationale": "Recoverability for a personal-finance app without a full trash UI, which the user judged unnecessary.", "consequences_positive": ["Accidental deletes recoverable", "Cheap to build on existing routes"], "consequences_negative": ["No UI yet to browse/restore older soft-deleted records"], "supersedes": "", "date": "20260530_213923"}
{"id": "adr_20260603_214500_7b2c", "title": "Broker sync reuses bank_synced_at; connection-type-agnostic model deferred", "status": "superseded", "context": "Scalable Capital balances (a broker, not a bank) are synced into assets via the same value-sync path as open-banking accounts. The Scalable sync stamps assets.bank_synced_at and the UI badge keys off (bank_synced_at set AND NOT bank_linked) to mean 'broker-synced'. The 'bank_' naming now describes more than banks.", "decision": "For the stopgap Scalable integration, reuse the existing bank_synced_at column and the bank_linked-derived signal rather than introducing a new sync-source abstraction. A future refactor should introduce a connection-type-agnostic model (e.g. a generic synced_at / sync_source on assets, or a polymorphic 'data source' link table) so bank, broker, and future sources are first-class.", "rationale": "The integration was an early stopgap; a schema/abstraction refactor would be wasted effort before the source stabilised. Reusing bank_synced_at kept the change small and let the existing freshness UI work unchanged.", "consequences_positive": ["Minimal change", "Existing freshness UI reused", "No migration churn for a stopgap"], "consequences_negative": ["Column/flag names (bank_synced_at, bank_linked) no longer match reality once a broker writes them", "Badge logic relies on the implicit 'synced but not bank_linked' convention", "Cash asset is anchored only by name+category (fragile to rename), unlike ISIN-anchored holdings"], "supersedes": "", "date": "20260603_214500"}
{"id": "adr_20260610_232830_77ef", "title": "In-app Scalable CLI login runs as a queued job with cache-backed status polling", "status": "accepted", "context": "The official Scalable CLI login (sc login) is a device-code flow: human_only, no JSON output, and it blocks while polling for the user's browser 2FA confirmation. Surfacing that flow in the Settings UI can't be done in a normal HTTP request — a blocking child would freeze the single-threaded dev server, and the activation URL must reach the UI in real time.", "decision": "Login runs in a queued job (RunScalableCliLogin, tries=1, timeout=900) in the existing queue:listen worker. The job runs `sc login` via Process::start, parses the activation URL + user code from plaintext stdout with a tolerant regex, and publishes them through ScalableLoginState (a cache wrapper at key scalable.login, ~20min TTL). The Settings card polls GET /scalable/cli/login/status (plain JSON, not Inertia) every 2s and shows the clickable URL + copyable code + spinner. Success is confirmed by `sc whoami --json` after exit 0 — the login's own text is never trusted. Card health uses a live whoami (cli_logged_in).", "rationale": "The worker is a blocking context by design, so it absorbs sc login's blocking without stalling requests. Cache (database driver, shared with the worker) holds the ephemeral URL/code with TTL so a crashed worker self-clears. Plaintext parsing is the only fragile part (mitigated by the pinned CLI version, a tolerant regex, raw-output logging, and the authoritative whoami gate). The terminal fallback (docker exec sc login) always remains.", "consequences_positive": ["No request ever blocks on sc login", "Real-time URL/code in the UI via lightweight JSON polling", "Success always verified by whoami, never by login text", "Crashed-worker pending self-expires via cache TTL + isInProgress staleness check"], "consequences_negative": ["Activation URL is parsed from human_only plaintext stdout — a CLI reword would leave it uncaptured (recoverable via retry/terminal)", "Adds a cache-backed transient state separate from scalable_connections", "queue:listen processes one job at a time, so a hung login occupies the worker until its 900s timeout"], "supersedes": "", "date": "20260610_232830"}
{"id": "adr_20260611_123842_32aa", "title": "Connection-agnostic sync columns: synced_at + sync_source replace bank_synced_at", "status": "accepted", "context": "adr_20260603_214500_7b2c deferred a naming fix: both the bank sync (Enable Banking) and the broker sync (Scalable) stamped assets.bank_synced_at, so the name lied, and the UI badge told bank from broker by exclusion (synced-but-not-bank_linked = broker). With the Scalable CLI now the permanent broker source, the debt was worth paying down.", "decision": "Renamed assets.bank_synced_at -> synced_at and added assets.sync_source ('bank'|'broker', NULL when never synced; constants Asset::SYNC_SOURCE_BANK/BROKER). FetchBankBalances stamps sync_source='bank', FetchScalableBalance stamps 'broker'. A single migration renames, adds the column, and backfills existing rows by the same active-link logic the app used (inlined, not via BankAccount::activeLinkKeys(), for migration-safety). The AssetTable/AssetForm badges now read sync_source explicitly instead of the exclusion heuristic. bank_linked stays (still derived in FetchAssetsByMonth) because it means 'an active connection manages this row NOW' (edit-lock/delete-warning) — a different signal from 'who last synced'. DisconnectController now clears both synced_at and sync_source.", "rationale": "Honest, explicit columns: the badge no longer guesses, and the schema no longer claims 'bank' for broker-synced rows. Keeping bank_linked separate preserves the live-link semantics the edit-lock needs.", "consequences_positive": ["Column names match reality", "Badge reads source explicitly, no exclusion guess", "Future sync sources are first-class via sync_source", "Disconnect fully clears sync state"], "consequences_negative": ["Cash asset is still anchored by name+category (unchanged by this work)"], "supersedes": "adr_20260603_214500_7b2c", "date": "20260611_123842"}
{"id": "adr_20260704_141500_a1f3", "title": "Adopt Prism for the AI advisor's model transport and tool-calling", "status": "accepted", "context": "The advisor talked to Ollama via a hand-written HTTP loop (OllamaAdvisorProvider) and was to gain a cloud Claude driver later; adding tool-calling meant writing a multi-step tool loop for each backend. The requirement is that local (Ollama) and cloud (Anthropic) are peers the user chooses at runtime.", "decision": "Introduce prism-php/prism (v0.100) as the LLM transport behind a single PrismAdvisorProvider implementing AdvisorProvider. The provider picks Provider::Ollama or Provider::Anthropic from services.advisor.driver; the tool-calling loop, message abstraction and streaming are Prism's. Advisor tools are defined once in App\\Advisor\\Tools\\AdvisorToolFactory (model-agnostic) and work with either backend. OllamaAdvisorProvider removed.", "rationale": "One tool definition + one loop serves both backends instead of two hand-written loops. Empirically validated: a spike showed qwen3:8b via Prism does clean tool round-trips with the <think> block stripped, and streaming works alongside tools. Prism pulls no per-provider SDKs (built on Laravel's HTTP client), keeping the dependency light in an otherwise minimal composer.json.", "consequences_positive": "Adding a provider is a config change, not a new loop; streaming + tool events unified; structured output available for free later.", "consequences_negative": "Prism is 0.x so the API can break between minors (pin the version); on Ollama only ToolChoice::Auto is available (fine for an advisor); the app now carries its first heavy dependency.", "supersedes": "", "date": "2026-07-04"}
{"id": "adr_20260705_090000_c4d2", "title": "Advisor generative UI via widget-emitting tools (not the Vercel AI SDK)", "status": "accepted", "context": "The advisor chat rendered every reply as markdown text. We wanted the model to CHOOSE to render rich, sometimes interactive components (starting with an interactive PAC simulator) instead of prose, without the app forcing it. A model outputs text, not React, so 'the model picks a component' must reduce to: the model emits a structured signal and the frontend maps it to a prebuilt component.", "decision": "Each advisor tool that has a UI counterpart, besides returning the annotated text the model reasons over, calls AdvisorWidgetCollector::add(type, data) with app-computed (deterministic) data. The collector is a request-scoped singleton (twin of AdvisorToolActivityReporter), armed by ContinueChat::complete on the pending assistant message; accumulated widgets are persisted to a new advisor_messages.widgets JSON column. All three message serializers (Index/Status/Message controllers) emit widgets. Frontend: Message.widgets union type, AdvisorWidgets dispatcher (switch on type), MessageBubble renders widgets UNDER the prose. First widget: pac_simulator, whose compound projection is mirrored in lib/pacMath.ts so sliders recompute live with no model round-trip.", "rationale": "The model chooses the component by choosing the tool it ALREADY calls; the numbers come from PHP (BuildAdvisorContext), so a weak local model cannot inject wrong figures into the UI (the recurring sign-flip footgun). Rejected the Vercel AI SDK generative-UI framework: same pattern, but it is a Node/JS SDK tied to streamText/streamUI in a Next.js route, whereas the advisor is Laravel+Prism (PHP) with a background-job+polling model (streaming froze the server) and 3 drivers already in PHP — adopting it means rewriting the whole advisor layer in Node for zero gain.", "consequences_positive": "One mechanism reuses the existing tool-loop and the activity-reporter pattern; works on all 3 drivers (Ollama/Regolo/Anthropic) because it is just a tool call; model cannot break widget shape; interactive widgets recompute client-side.", "consequences_negative": "The PAC compound formula is duplicated PHP<->TS (kept in step by tests on both sides); adding a widget touches ~6 files (tool, type union, component, dispatcher).", "supersedes": "", "date": "2026-07-05"}
{"id": "adr_20260719_134115_9b2e", "title": "Advisor memory is the one profile field written autonomously (no confirmation card)", "status": "accepted", "context": "The advisor follows an 'AI proposes, user applies' boundary: every profile change (name, horizon, risk tolerance, income, emergency fund, birth date, risk-notes) is emitted as a one-click confirmation card (profile_proposal widget, POST /advisor/profile) and never written silently. The durable-memory field (preferences/facts to remember, e.g. 'non vuole obbligazioni') originally rode the same cards via a `memory` param on propose_profile_update / confirm_profile_fact. The user found the per-fact card friction annoying for what is a low-stakes note, and asked that memory be kept up to date by the AI on its own, told after the fact.", "decision": "Memory is the SINGLE profile field the advisor updates autonomously. A dedicated tool remember_fact(fact) writes it immediately (append-merge via mergedMemory: bulleted, de-duplicated, clamped to the column limit) and returns text instructing the model to tell the user what it stored. The `memory` param was removed from propose_profile_update and confirm_profile_fact, so memory no longer routes through any card. All other profile fields keep the confirmation-card flow unchanged.", "rationale": "Memory is additive, low-stakes, and never overwrites prior facts, so a confirmation gate added friction without protecting anything — unlike horizon/risk/income where a wrong value skews the whole analysis. Writing autonomously and reporting back ('Ho memorizzato che...') matches how the user wanted it to feel. remember_fact reuses the isProfileFactAllowed gate so it only writes during a real chat turn (no-op on report generation / unit tests where no assistant message is armed), keeping the write side-effect scoped.", "consequences_positive": ["No card friction for durable preferences; the advisor keeps memory current mid-conversation.", "The AI-proposes-user-applies boundary stays intact for every consequential field.", "Append-merge (mergedMemory) still guarantees earlier facts survive and duplicates are skipped."], "consequences_negative": ["One profile field now writes without explicit user confirmation; mitigated by it being additive-only and always reported back in the reply.", "The model must be steered (both chat prompts) to route durable facts to remember_fact and everything else to the cards; a confused model could mis-route."], "supersedes": "", "date": "2026-07-19"}
{"id": "adr_20260728_220339_6457", "title": "Investor profile lives on the Goal page; the horizon is derived from the goal's target date", "status": "accepted", "context": "The investor profile (name, birth date, horizon, risk tolerance, notes, memory) was only reachable through a 'Profilo' button inside the AI advisor page, while the Goal (target value, target date, allocation, milestones) had its own nav section. The two describe one thing — who the user is and where they are heading — but sat far apart, and two fields overlapped: InvestorProfile.horizon (short/medium/long, hand-picked) and Goal.target_date said the same thing with no synchronisation, so a profile could claim 'breve' beside a 2040 goal. Both statements were rendered into the SAME advisor prompt (RenderAdvisorContext::profileSection + the goal briefing), leaving the model to guess which to believe. ContinueChat's interview slots already treated target date and horizon as one concept ('date' slot matches both a year and 'entro N anni'), so the unification was half-present.", "decision": "The Goal page owns the profile: FetchGoalData exposes a `profile` prop, Components/Goal/ProfileCard renders it above the goal progress (and above the empty state when no goal exists), and editing opens the existing Advisor/ProfileDialog so there is one form and one write path (POST /advisor/profile). The advisor's 'Profilo' button became 'Profilo e obiettivo' linking to /goal; the now-dead ProfileDialog mount and the never-used ProfileSummary were deleted. The horizon became derived: Goal::horizon() buckets the years to target_date (<3 short, 3-10 medium, 10+ long, null without a date, 'short' once elapsed). It was removed from InvestorProfile::$fillable, from StoreInvestorProfileRequest, and as a parameter of propose_profile_update / confirm_profile_fact; both tool descriptions now steer the model to propose_goal_core instead. The DB column stays (holding the last hand-entered value) but nothing reads it. ProfileProposal filters its POST through a WRITABLE_FIELDS allowlist so sessions persisted before the change cannot resend a stale horizon.", "rationale": "Risk tolerance was NOT unified: it has an independent meaning (tolerance for a drawdown) and a real consumer — AdvisorToolFactory::expectedAnnualReturn maps low/medium/high to 3/5/7% real return, which compounds across the whole projection. Horizon had no such consumer: it only ever reached a line of prompt text, while target_date carried the same fact to the day. Deriving it removes a contradiction from the advisor prompt (the feature with the most invested work) and one hand-maintained field, without touching the field that does real work.", "consequences_positive": ["One source of truth for the horizon; the prompt can no longer contradict itself, and the rendered line says where the value comes from.", "Profile and goal are visually and conceptually adjacent; one edit surface instead of two.", "One less field to keep in sync by hand; moving the goal date updates the horizon everywhere.", "The stale-horizon path from old sessions is covered by a test, not just by the server ignoring the field."], "consequences_negative": ["The horizon can no longer be stated independently of a goal: with no goal (or no target date) it reads '—' and the advisor is told nothing.", "The legacy investor_profile.horizon column lingers as dead data; a reader going straight to the DB could mistake it for live.", "Goal::horizon() duplicates the bucket thresholds that the UI labels also spell out (ProfileCard / ProfileDialog HORIZON_RANGES)."], "supersedes": "", "date": "2026-07-28"}
{"id": "adr_20260803_145900_9f31", "title": "Cashflow pages by month server-side; whole-history aggregates move to server props", "status": "accepted", "context": "Cashflow\\IndexController shipped the entire bank_transactions table on every visit to /cashflow, and the page grouped the rows by day filtering them in memory with two date inputs. The payload grew with every sync and the day-grouped list was unbounded. The emergency-fund coverage and the two 'media / mese' cards were computed client-side over that full array.", "decision": "The page shows one month at a time, selected with a ?month=YYYY-MM-01 query param and a prev/select/next control, reusing the convention InputData (Bilancio) already established: a FormRequest with a defaulting month() accessor, an Action returning the whole prop array, and an Inertia visit. Cashflow deviates on the visit itself: it uses a partial reload (only: ['transactions', 'month'], preserveState + preserveScroll) instead of Bilancio's full preserveState: false visit, because the page holds staged edits and filters that must survive a month change, and the other props are month-independent. Future months are unreachable: the next arrow is disabled at the current month, the select lists only past months, and the FormRequest clamps a hand-typed future month to the current one. Prop building moved out of the controller into App\\Actions\\Cashflow\\FetchCashflowData. The date-range inputs were removed; the month selector is the only period control. The whole-history figures now come from props, computed by the pre-existing ComputeMonthlyExpense and ComputeMonthlySalary actions, plus App\\Actions\\Transactions\\FetchTransactionMonths for the month list.", "rationale": "A single month is the natural unit for reviewing bank transactions, and the averages cannot be derived from one month's rows — they had to move server-side regardless. Reusing the ?month= pattern kept a second period-navigation idiom out of the codebase.", "consequences_positive": ["Bounded payload and bounded list, independent of history length.", "Cashflow's Index controller now follows the controller-delegates-to-Action convention like every other Index page.", "The averages and the emergency-fund coverage are stable numbers that no longer shift with a UI filter.", "A booking_date index was added: the pre-existing composite index leads with bank_account_id and cannot serve a month range scan."], "consequences_negative": ["The partial reload keeps staged edits alive across months, so a save can post edits belonging to a month that is no longer displayed. Harmless — the PATCH is keyed by transaction id — but the confirmation bar is the only cue that they are still pending.", "The emergency-fund coverage no longer reflects unsaved edits — it updates after saving, when the page reloads.", "The server-side averages ignore rows with a NULL flow_type (never classified), where the old client-side calculation counted them as expenses."], "supersedes": null, "date": "2026-08-03"}
{"id": "adr_20260803_152600_b4e7", "title": "The Investments section is absorbed into Bilancio, renamed 'Bilancio investimenti'", "status": "superseded", "context": "/investments was a page consuming a single prop (ComputePositionReturns' output) that rendered an aggregate summary, a positions table and a CSV-export button. Its per-position detail was already reachable from Bilancio: AssetTable opens the same TransactionsDialog on any transaction_managed asset. Its aggregate was already rendered by the Dashboard (PortfolioInsights), the Advisor prompt and the Advisor list_positions widget. Its CSV-export button was already duplicated in Settings under Dati -> Import/Export.", "decision": "The positions table and the aggregate move into /input as Components/Data/PositionsCard.tsx, fed by a new positionReturns prop on FetchInputData. The Investments page, its Analytics\\InvestmentsController and the /investments route are deleted. The nav entry is removed and the /input label becomes 'Bilancio investimenti'. The duplicate CSV button is dropped, not moved: the Settings one already covers it.", "rationale": "The data belongs where the assets are logged; a separate section for one prop's worth of numbers was navigation cost without content. Deleting the duplicated export button removed a second entry point to the same endpoint.", "consequences_positive": ["One fewer nav entry and one fewer page for the same information.", "The positions card renders nothing when no asset is transaction-managed, so Bilancio is unchanged for a user with no broker import.", "File names and routes stay put (InputData.tsx, /input, input.index, Assets\\IndexController), consistent with the documented convention that file names don't track UI titles."], "consequences_negative": ["The positions figures are whole-history and ISIN-deduplicated while every other prop on the page is scoped to the selected month; the card states this explicitly, but it is a mixed-scope page.", "The aggregate is now rendered in three places (positions card, Dashboard PortfolioInsights, Advisor) from the same action.", "/export/csv keeps only the Settings entry point."], "supersedes": null, "date": "2026-08-03"}
{"id": "adr_20260804_181500_2c8a", "title": "The Cashflow page leads with metrics; the transaction list moves behind a dialog", "status": "accepted", "context": "/cashflow had grown into a transaction editor wearing a dashboard's clothes: the per-day transaction list occupied most of the page while the figures the page exists for (average salary, average expense, emergency-fund coverage) were four small tiles above it. The list's height also drove a chain of layout failures — a card squeezed to zero height with its rows rendering outside the visible box, patched with a min-h floor. The owner stated the actual purpose: the page is for knowing average expenses, salary and the emergency fund; the transactions are a detail, interesting only for what is derived from them.", "decision": "Invert the hierarchy. The page shows, in order: three whole-history metric cards (average salary, average expense, and average saving = salary - expense), the emergency fund as the visual headline (coverage in months, shortfall, and months-to-target at the average saving rate), a monthly net bar chart for trend, a compact card for the shown month, and PositionsCard. The transaction list and its filters move into TransactionsReviewDialog, opened by a button that shows the row count and flags staged edits. The staged-edits Map stays on the page, not in the dialog, so closing it doesn't discard work.", "rationale": "Matching the page's layout to its stated purpose fixes the layout problems at the root rather than with another height floor: with no list competing for height, the flex-1/min-h-0 chain and the min-h floor all disappear and the page simply scrolls like Dashboard and Settings. The average-saving figure is derived from two numbers already shown, so it adds a genuinely useful metric at no data cost.", "consequences_positive": "The page answers 'how much do I earn, spend, keep, and how is my fund doing' without scrolling. Layout is ordinary flow, no height contention. Review is still one click away and pending edits are visible on the button.", "consequences_negative": "The monthly trend chart reads saved state only, so it does not reflect staged unsaved edits, while the shown month's totals do — two calculation paths that must stay in agreement (BuildMonthlyFlowSeries mirrors the client's effective() filters, including treating a null flow_type as an expense). Correcting many rows now costs an extra click to open the dialog.", "supersedes": null, "date": "2026-08-04"}
{"id": "adr_20260804_185500_3d67", "title": "The positions card is deleted: its detail and its aggregate were both already reachable", "status": "accepted", "context": "PositionsCard (per-position shares, average cost, cost basis, value, P&L, plus an aggregate) had been tried on /investments, then on /input, then on /cashflow, each time looking for a home and never fitting. On /cashflow it was plainly off-topic — that page is about salary, average expenses and the emergency fund — and being tall and content-sized it was what kept the page from holding a full-height layout. Looking for the right page turned up the reason none existed: every figure it showed was already available elsewhere. The per-position detail is rendered in full by TransactionsDialog, opened from any transaction_managed asset in the Bilancio, which already shows shares, average cost, cost basis, current value, unrealised P&L with percentage and realised P&L. The aggregate is rendered by the Dashboard's PortfolioInsights as the 'Rendimento investimenti' insight, and is also in the Advisor prompt and its get_position / list_positions tools. The owner confirmed consulting these numbers rarely, out of curiosity.", "decision": "Delete PositionsCard.tsx and its test, drop the positionReturns prop and the ComputePositionReturns dependency from FetchCashflowData, and replace CashflowIndexTest's two positionReturns tests with one asserting the prop is absent. ComputePositionReturns stays where it is consumed: FetchDashboardData, and the Advisor via the Dashboard context.", "rationale": "The placement question had no answer because the premise was wrong: the card was a parallel structure over data that already had a home. Per-asset figures belong on that asset's own row, which is where the dialog already puts them.", "consequences_positive": ["One component and 10 tests less, with no data lost.", "/cashflow no longer runs a whole-history, ISIN-deduplicated computation on every page load for a card consulted rarely.", "The card was the reason the page needed a min-h floor and could not hold a full-height layout; that constraint is gone."], "consequences_negative": ["The overview — all positions side by side in one table — is gone; the detail is now reached one asset at a time.", "The aggregate on the Dashboard is one insight in a rotating carousel, so it is not permanently on screen."], "supersedes": "adr_20260803_152600_b4e7", "date": "2026-08-04"}
