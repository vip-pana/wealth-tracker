{"_schema": "adr", "_version": "1.0", "_description": "Architectural decision records: routing, state management, build pipeline, major library choices, cross-cutting refactors. Append only; supersede with a new entry instead of editing.", "_fields": ["id", "title", "status", "context", "decision", "rationale", "consequences_positive", "consequences_negative", "supersedes", "date"]}
{"id": "adr_20260530_213923_014a", "title": "No REST API — Inertia.js with server-rendered React", "status": "accepted", "context": "Single-user local app; a separate JSON API plus a client fetch layer would add ceremony with no payoff.", "decision": "Controllers return Inertia::render() for GET and redirect()->back() for mutations; React pages receive typed props directly. No JSON API.", "rationale": "Removes the API/client contract and serialization boundary; one source of truth for props; simpler to keep TypeScript types aligned.", "consequences_positive": ["Less code", "No API versioning", "Typed props end-to-end"], "consequences_negative": ["No external/programmatic API surface", "Coupled to Inertia"], "supersedes": "", "date": "20260530_213923"}
{"id": "adr_20260530_213923_014a", "title": "Soft deletes plus toast-level undo for user data", "status": "accepted", "context": "An assistant once deleted real financial data; deletions must be recoverable.", "decision": "Core tables (assets, categories, goals, pension) use SoftDeletes; destroy controllers flash an undo URL that a restore endpoint consumes.", "rationale": "Recoverability for a personal-finance app without a full trash UI, which the user judged unnecessary.", "consequences_positive": ["Accidental deletes recoverable", "Cheap to build on existing routes"], "consequences_negative": ["No UI yet to browse/restore older soft-deleted records"], "supersedes": "", "date": "20260530_213923"}
{"id": "adr_20260603_214500_7b2c", "title": "Broker sync reuses bank_synced_at; connection-type-agnostic model deferred", "status": "superseded", "context": "Scalable Capital balances (a broker, not a bank) are synced into assets via the same value-sync path as open-banking accounts. The Scalable sync stamps assets.bank_synced_at and the UI badge keys off (bank_synced_at set AND NOT bank_linked) to mean 'broker-synced'. The 'bank_' naming now describes more than banks.", "decision": "For the stopgap Scalable integration, reuse the existing bank_synced_at column and the bank_linked-derived signal rather than introducing a new sync-source abstraction. A future refactor should introduce a connection-type-agnostic model (e.g. a generic synced_at / sync_source on assets, or a polymorphic 'data source' link table) so bank, broker, and future sources are first-class.", "rationale": "The integration is a throwaway stopgap until the official Scalable CLI is allowlisted; a schema/abstraction refactor would be wasted effort before the real source lands. Reusing bank_synced_at kept the change small and let the existing freshness UI work unchanged.", "consequences_positive": ["Minimal change", "Existing freshness UI reused", "No migration churn for a stopgap"], "consequences_negative": ["Column/flag names (bank_synced_at, bank_linked) no longer match reality once a broker writes them", "Badge logic relies on the implicit 'synced but not bank_linked' convention", "Cash asset is anchored only by name+category (fragile to rename), unlike ISIN-anchored holdings"], "supersedes": "", "date": "20260603_214500"}
{"id": "adr_20260610_225919_aa56", "title": "Official Scalable CLI is the primary balance source; proxy kept as fallback", "status": "superseded", "context": "The official Scalable Capital CLI (sc) was allowlisted and runs headless in the container once its session uses the file backend (config.toml [auth] session_backend/signing_key_backend = file; the container has no keyring/DBus). It supersedes the unofficial host proxy as the balance source, but the proxy still works and the CLI login is interactive (device code + 2FA, human_only).", "decision": "Add ScalableCliClient (read-only: broker overview/holdings/whoami via an allowlist, never trade) alongside ScalableUnofficialClient; both implement a ScalableSource interface. FetchScalableBalance picks one source per run via services.scalable.source: 'auto' (default) prefers a logged-in CLI (probed with whoami) and falls back to the proxy, 'cli'/'proxy' pin one. Holdings match assets by ISIN as before; cash is derived as overview total - securities - crypto (no longer a fragile name-only proxy lookup). CLI login stays a manual terminal step (docker exec sc login) for now; in-app device-code login is a separate follow-up.", "rationale": "The CLI is official and lower-risk than the ToS-violating proxy, but its session lapses and login can't be automated from PHP. Keeping the proxy as an automatic fallback means a lapsed CLI session degrades gracefully instead of failing the nightly sync. One source per run keeps positions and cash coherent.", "consequences_positive": ["Official source preferred", "Graceful fallback on lapsed CLI session", "Cash derived from overview totals, more robust than name lookup", "Trade commands structurally unreachable via the allowlist"], "consequences_negative": ["CLI login still manual (terminal device code) until the in-app flow lands", "config.toml file backend must be seeded into the scalable-cli volume (done in the dev compose command, not the Dockerfile, to defeat volume shadowing)", "bank_synced_at naming debt from adr_20260603_214500_7b2c still unaddressed"], "supersedes": "", "date": "20260610_225919"}
{"id": "adr_20260610_232830_77ef", "title": "In-app Scalable CLI login runs as a queued job with cache-backed status polling", "status": "accepted", "context": "The official Scalable CLI login (sc login) is a device-code flow: human_only, no JSON output, and it blocks while polling for the user's browser 2FA confirmation. Surfacing that flow in the Settings UI can't be done in a normal HTTP request — a blocking child would freeze the single-threaded dev server, and the activation URL must reach the UI in real time.", "decision": "Login runs in a queued job (RunScalableCliLogin, tries=1, timeout=900) in the existing queue:listen worker. The job runs `sc login` via Process::start, parses the activation URL + user code from plaintext stdout with a tolerant regex, and publishes them through ScalableLoginState (a cache wrapper at key scalable.login, ~20min TTL). The Settings card polls GET /scalable/cli/login/status (plain JSON, not Inertia) every 2s and shows the clickable URL + copyable code + spinner. Success is confirmed by `sc whoami --json` after exit 0 — the login's own text is never trusted. One login button follows the active source (CLI when source is cli/auto, else the proxy login). Card health uses a live whoami (cli_logged_in) when the CLI is the source.", "rationale": "The worker is a blocking context by design, so it absorbs sc login's blocking without stalling requests. Cache (database driver, shared with the worker) holds the ephemeral URL/code with TTL so a crashed worker self-clears. Plaintext parsing is the only fragile part (mitigated by the pinned CLI version, a tolerant regex, raw-output logging, and the authoritative whoami gate). The terminal fallback (docker exec sc login) always remains.", "consequences_positive": ["No request ever blocks on sc login", "Real-time URL/code in the UI via lightweight JSON polling", "Success always verified by whoami, never by login text", "Crashed-worker pending self-expires via cache TTL + isInProgress staleness check"], "consequences_negative": ["Activation URL is parsed from human_only plaintext stdout — a CLI reword would leave it uncaptured (recoverable via retry/terminal)", "Adds a cache-backed transient state separate from scalable_connections", "queue:listen processes one job at a time, so a hung login occupies the worker until its 900s timeout"], "supersedes": "", "date": "20260610_232830"}
{"id": "adr_20260611_120752_694f", "title": "Remove the unofficial Scalable proxy; the official CLI is the sole source", "status": "accepted", "context": "The official Scalable CLI was rolled out (adr_20260610_225919_aa56) alongside the unofficial host proxy as a fallback. After running the CLI as the primary source with in-app device-code login (adr_20260610_232830_77ef), the proxy is no longer needed: it is unofficial, carries ToS/ban risk, and was always a throwaway stopgap.", "decision": "Delete the proxy entirely. Removed from the repo: ScalableUnofficialClient, the ScalableSource interface (FetchScalableBalance now depends on ScalableCliClient directly), Scalable/LoginController + route POST /scalable/login, the services.scalable.source/balance_url/token config and their env keys, the proxy branch in the Settings card, and the host-side infra (scripts/scalable-proxy-setup.sh, scripts/scalable-proxy/, docs/scalable-proxy.md). FetchScalableBalance is now CLI-only: inert unless services.scalable.cli.enabled, fails the run (recording SYNC_FAILED) when the CLI session has lapsed. The host proxy install (~/scalable-unofficial/ + its launchd agent) was also removed from the Mac.", "rationale": "One official source is simpler and safer than maintaining a fragile, ToS-risky fallback. The CLI session is long-lived and re-login is a one-click in-app flow, so the fallback bought little.", "consequences_positive": ["Single official data source", "No ToS/ban risk", "Less code and infra to maintain", "No source-selection indirection"], "consequences_negative": ["A lapsed CLI session means no sync until the user reconnects (no automatic fallback)", "bank_synced_at naming debt from adr_20260603_214500_7b2c still unaddressed"], "supersedes": "adr_20260610_225919_aa56", "date": "20260611_120752"}
{"id": "adr_20260611_123842_32aa", "title": "Connection-agnostic sync columns: synced_at + sync_source replace bank_synced_at", "status": "accepted", "context": "adr_20260603_214500_7b2c deferred a naming fix: both the bank sync (Enable Banking) and the broker sync (Scalable) stamped assets.bank_synced_at, so the name lied, and the UI badge told bank from broker by exclusion (synced-but-not-bank_linked = broker). With the Scalable CLI now the permanent broker source, the debt was worth paying down.", "decision": "Renamed assets.bank_synced_at -> synced_at and added assets.sync_source ('bank'|'broker', NULL when never synced; constants Asset::SYNC_SOURCE_BANK/BROKER). FetchBankBalances stamps sync_source='bank', FetchScalableBalance stamps 'broker'. A single migration renames, adds the column, and backfills existing rows by the same active-link logic the app used (inlined, not via BankAccount::activeLinkKeys(), for migration-safety). The AssetTable/AssetForm badges now read sync_source explicitly instead of the exclusion heuristic. bank_linked stays (still derived in FetchAssetsByMonth) because it means 'an active connection manages this row NOW' (edit-lock/delete-warning) — a different signal from 'who last synced'. DisconnectController now clears both synced_at and sync_source.", "rationale": "Honest, explicit columns: the badge no longer guesses, and the schema no longer claims 'bank' for broker-synced rows. Keeping bank_linked separate preserves the live-link semantics the edit-lock needs.", "consequences_positive": ["Column names match reality", "Badge reads source explicitly, no exclusion guess", "Future sync sources are first-class via sync_source", "Disconnect fully clears sync state"], "consequences_negative": ["Cash asset is still anchored by name+category (unchanged by this work)"], "supersedes": "adr_20260603_214500_7b2c", "date": "20260611_123842"}
